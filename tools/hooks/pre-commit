#!/bin/sh
# Block commits that add KVKK personal data or secrets (standards/data-policy.md).
PY=$(command -v python || command -v python3 || command -v py) || { echo "guard: python not found" >&2; exit 1; }
exec "$PY" "$(git rev-parse --show-toplevel)/tools/graph.py" guard --staged
