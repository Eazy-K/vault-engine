#!/bin/sh
# Block commits that add KVKK personal data or secrets (the engine's defaults/standards/data-policy.md).
HOOK_DIR=$(dirname "$0")
. "$HOOK_DIR/_find-python.sh"
exec "$PYTHON_CMD" $PYTHON_ARGS "$HOOK_DIR/../graph.py" guard --staged
