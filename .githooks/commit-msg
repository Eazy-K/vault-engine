#!/bin/sh
# Same leak check for the commit message itself.
HOOK_DIR=$(dirname "$0")
PY=$(command -v python || command -v python3 || command -v py) || { echo "leakcheck: python not found" >&2; exit 1; }
exec "$PY" "$HOOK_DIR/../tools/graph.py" leakcheck --message-file "$1"
